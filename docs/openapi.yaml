openapi: 3.1.0
info:
  title: CampusForge API
  version: 0.1.0
servers:
  - url: http://localhost:4000/v1
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
