# CampusForge API

Fastify API scaffold. It keeps server setup, security middleware, environment parsing, and a health endpoint in place.
Product routes should be added by the implementation team.

## Local development

```bash
pnpm --filter @campusforge/api dev
```

## Available endpoint

```text
GET /v1/health
```
