# CampusForge Web

Next.js App Router scaffold for the CampusForge frontend. It intentionally contains only the root page, global styles,
and application layout so the product UI can be implemented on top of a clean framework.

## Local development

```bash
cp ../../config.example.toml ../../config.toml
pnpm --filter @campusforge/web dev
```
